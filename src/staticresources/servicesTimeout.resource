
(function (global) {

    'use strict';

    global.svcTimeout = function (token, seconds, popSeconds, refreshFunction, $, moment) {
        //alert('The session has timed out!! This is where the counter will come!');
    };

}(this));